"use strict";

// Task-05: Use the `shift` method to remove the first element from the array and log the updated array.

const arr = [1, 2, 3, 4, 5];

console.log("Before Shift Operation:");
console.log(arr);

arr.shift(0);

console.log();
console.log("After Shift Operation:");
console.log(arr);
